import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#fff',
    alignItems: 'center',
    paddingTop: 200,
    paddingLeft: 25,
    paddingRight: 25
  },
  text: {
    fontSize: 36
  }
});

export default styles;
